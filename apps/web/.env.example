# ========================================
# XINGU WEB - Frontend Environment Template
# ========================================
# Copy this file to .env.local for development
# For production, use .env.production

# ========================================
# API ENDPOINTS (Development)
# ========================================
# Auth Service
NEXT_PUBLIC_API_AUTH_URL=http://localhost:3001/api/auth

# Template Service
NEXT_PUBLIC_API_TEMPLATE_URL=http://localhost:3002/api/templates

# Game Service
NEXT_PUBLIC_API_GAME_URL=http://localhost:3003/api/games

# Room Service
NEXT_PUBLIC_API_ROOM_URL=http://localhost:3004/api/rooms

# Result Service
NEXT_PUBLIC_API_RESULT_URL=http://localhost:3006/api/results

# WebSocket Service
NEXT_PUBLIC_WS_URL=http://localhost:3005

# ========================================
# SENTRY ERROR TRACKING
# ========================================
# Frontend Sentry DSN (NEXT_PUBLIC_ prefix makes it available in browser)
# Get your DSN from: https://sentry.io/settings/projects/
# NEXT_PUBLIC_SENTRY_DSN=https://xxxxx@xxxxx.ingest.sentry.io/xxxxx

# Server-side Sentry DSN (keep private, not exposed to browser)
# SENTRY_DSN=https://yyyyy@yyyyy.ingest.sentry.io/yyyyy

# Optional: Track releases
# NEXT_PUBLIC_VERCEL_GIT_COMMIT_SHA=auto-populated-by-ci

# ========================================
# NOTES
# ========================================
# 1. Variables with NEXT_PUBLIC_ prefix are exposed to the browser
# 2. Keep SENTRY_DSN (without NEXT_PUBLIC_) private for server-side only
# 3. In production, update URLs to your actual domain
# 4. Sentry is automatically disabled in development (see sentry config files)
